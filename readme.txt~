This is a java program that implements a bounded queue using an array. To compile, use command 'make'
in command line. 

A jUnit test is included with small inputs. To test larger inputs, simply add data using a loop.
To test, use command 'make check' in commandline. 
